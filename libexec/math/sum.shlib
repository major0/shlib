#!/usr/bin/env shlib

import math/calc

sum() { IFS='+' calc "$@"; }

shlib_main {
	for arg; do
		case "${arg}" in (-h|-help|--help)
			echo 'usage: sum <numbers>'
			exit 0
		esac
	done
	sum "$@"
}

# vim: filetype=sh
